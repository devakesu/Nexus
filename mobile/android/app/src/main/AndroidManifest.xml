<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required on Android 13+ for showing push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required for recording voice messages in chat -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Required for taking photos to send in chat -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Required for sharing current location in chat -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Required to read the phone number of a contact picked from the device
         address book when adding a Safety Center trusted contact -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!-- Required so the Meetup Safety check-in alert can launch full-screen
         over the lock screen (see checkin_alert_screen.dart) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- Required so the Meetup Safety check-in deadline fires via AlarmManager
         even if Android kills the app process between check-ins -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Required to wake the screen for the full-screen check-in alert -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Required to place an SOS call to 112 directly, with zero taps,
         instead of just opening the dialer pre-filled (see safety_dialer.dart) -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <!-- Required to run SafetyRecordingService, which keeps the app process
         alive while Silent SOS's Digital Witness recording continues in the
         background (screen locked / app backgrounded) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="${appScheme}" android:host="home"/>
                <data android:scheme="${appScheme}" android:host="login"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="${appHost}" android:pathPrefix="/app"/>
                <data android:scheme="https" android:host="${appHost}" android:pathPrefix="/login-callback"/>
            </intent-filter>
        </activity>
        <!-- Required by image_cropper (uCrop) for the square-crop activity -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <!-- Keeps the app process alive during Silent SOS's Digital Witness
             recording once the app is backgrounded/locked - see
             SafetyRecordingService.kt and digital_witness_recorder.dart -->
        <service
            android:name=".SafetyRecordingService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone"/>
        <!-- Fallback FCM channel for payloads that don't specify a channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="likes_like" />
        <!-- TODO: Replace with a real Google Maps Platform API key before
             shipping chat location sharing - see AppDelegate.swift for the
             iOS side. Consider a separate key per flavor (nexus/nexus-mec). -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for Spotify native SSO (Android 11+ package visibility) -->
        <package android:name="com.spotify.music" />
        <package android:name="com.spotify.lite" />
        <!-- Required so url_launcher's canLaunchUrl can find the dialer app
             for tel: links (Safety Center helplines / Call 112) on
             Android 11+, where package visibility hides it otherwise. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Required for mailto: links in WebViews / UI -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
