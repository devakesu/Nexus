extensions:
  - addsTo:
      pack: codeql/python-all
      extensible: barrierModel
    data:
      # Tuple format: [module_path, target_member, threat_kind]
      - ["app.core.utils.sms", "Member[redact_phone]", "clear-text-logging"]
